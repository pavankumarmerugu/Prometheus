apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: monitoring
spec:
  selector:
    app: grafana-server
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
